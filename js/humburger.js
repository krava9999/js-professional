class Hamburger {
    constructor(size, stuffing) { 
        this.size = size;
        this.stuffing = stuffing;
     }

    //  render(){
    //      return `<input type="radio" name="burger"> small
    //              <input type="radio" name="burger" checked> big
    //              `
    //  }
  }

// const menu = new Hamburger();
// let container = document.querySelector(".menu");
// container.insertAdjacentHTML('beforeend', menu.render());